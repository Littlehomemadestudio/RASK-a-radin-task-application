# Rask desktop dependencies.
#
# Required (auto-installed on first run if missing, but pre-install recommended):
#   pip install -r requirements.txt
#
# Core UI stack:
customtkinter>=5.2.2
Pillow>=10.0.0

# Encryption + hashing (AES-256-GCM backups, PBKDF2 PINs):
cryptography>=42.0

# PDF export:
reportlab>=4.0

# Optional — enables voice input:
# speech_recognition>=3.10
# pyaudio>=0.2.14

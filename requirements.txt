# Build dependencies for host (NOT bundled into APK)
# These are installed by build_apk.py into .venv/ automatically.
# This file is for manual `pip install -r requirements.txt` users.
buildozer==1.5.0
Cython==0.29.36
virtualenv==20.26.1
pip>=23.0
setuptools>=68.0
wheel>=0.40
sh==2.0.7
Pillow==10.3.0

# Runtime deps (also in buildozer.spec requirements=) for desktop testing
kivy==2.3.0
kivymd==1.2.0
pyjnius==1.6.1
cryptography==42.0.5
python-dateutil==2.9.0
